<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tk.mybatis.simple.mapper.RoleAuthorityServiceI">
    <resultMap id="BaseResultMap" type="tk.mybatis.simple.model.entity.RoleAuthority">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="authority_id" jdbcType="INTEGER" property="authorityId"/>
        <result column="role_id" jdbcType="INTEGER" property="roleId"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete from role_authority
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="tk.mybatis.simple.model.entity.RoleAuthority">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into role_authority (authority_id, role_id)
        values (#{authorityId,jdbcType=INTEGER}, #{roleId,jdbcType=INTEGER})
    </insert>
    <update id="updateByPrimaryKey" parameterType="tk.mybatis.simple.model.entity.RoleAuthority">
        update role_authority
        set authority_id = #{authorityId,jdbcType=INTEGER},
        role_id = #{roleId,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select id, authority_id, role_id
        from role_authority
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select id, authority_id, role_id
        from role_authority
    </select>
</mapper>